import React, { useState } from 'react';

const ContactForm7 = ({ formID, children }) => {
  const [formData, setFormData] = useState({});
    return <div>{children}</div>
};

export default ContactForm7;